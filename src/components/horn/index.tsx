import { View } from '@tarojs/components';
import React from 'react';

export interface HornProps {

}

const Horn: React.FC<HornProps> = ({

}) => {
  return (
    <View>

    </View>
  )
}

export default Horn